version: 0.1
   
phases:
  build:
    commands:
      - npm install
      - echo " Lint"
      - echo "Building the Code"
      - npm run-script build
artifacts:
  files:
    - '**/*'
  discard-paths: no        